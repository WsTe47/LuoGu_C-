	//#include<iostream>
	//using namespace std;
	//int main()
	//{
	//	double n = 1,S=0;
	//	double a[1000];
	//	cin >> n;
	//	for (int i = 0; i < n; i++)
	//		cin >> a[i];
	//	double max = 0, min = 11;
	//	for (int i = 0; i < n; i++)
	//	{
	//		if (a[i] > max)
	//			max = a[i];
	//		if (a[i] < min)
	//			min = a[i];
	//	}
	//	for (int i = 0; i < n; i++)
	//		S += a[i];
	//	double m = 0;
	//	m = (S - max - min) / (n - 2);
	//	printf("%0.2f", m);
	//
	//	return 0;
	//}